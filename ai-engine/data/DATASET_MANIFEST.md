# Dataset Manifest Location

The authoritative source manifest is [`../../data/DATASET_MANIFEST.md`](../../data/DATASET_MANIFEST.md).
It describes the immutable datasets read from `../../data/raw`. This pointer is
kept inside the AI engine to avoid maintaining a divergent copy. Verified audit
results and source checksums are in `../reports/data_quality_report.json`.


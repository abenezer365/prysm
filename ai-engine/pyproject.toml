[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "prysm-ai-engine"
version = "0.2.0"
description = "Prysm financial intelligence engine"
requires-python = ">=3.11"
dependencies = ["numpy>=2.0", "pandas>=2.2", "pyarrow>=15", "fastapi>=0.115,<0.120", "pydantic>=1.10,<2", "uvicorn>=0.35,<1"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

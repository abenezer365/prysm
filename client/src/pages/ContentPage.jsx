import { Link, useLocation } from 'react-router-dom'; import { ArrowRight } from 'lucide-react'; import { pageContent } from '../config/content'; import DemoGraph from '../components/DemoGraph';
export default function ContentPage(){const{pathname}=useLocation();const [title,intro]=pageContent[pathname]||['Page in development','This section has an intentional extension point for verified content.'];const playground=pathname==='/intelligence/playground';return <div className="shell py-20"><p className="eyebrow">Prysm Intelligence / {pathname.split('/')[1]||'public'}</p><h1 className="page-title mt-5 max-w-4xl">{title}</h1><p className="muted mt-7 max-w-3xl text-xl leading-8">{intro}</p>{playground&&<div className="mt-12"><DemoGraph/></div>}<div className="mt-16 grid gap-px overflow-hidden rounded-xl border border-[var(--border)] bg-[var(--border)] md:grid-cols-3">{[['Context first','Understand where information came from and the time boundary around it.'],['Human oversight','Investigative judgment remains accountable to people and policy.'],['Designed to expand','Content and integrations can grow without changing the core route.']].map(([t,d])=><article className="bg-[var(--surface)] p-7" key={t}><h2 className="font-semibold">{t}</h2><p className="muted mt-3 text-sm leading-6">{d}</p></article>)}</div><Link to="/docs" className="mt-10 inline-flex items-center gap-2 font-semibold text-[var(--accent)]">Read the system documentation <ArrowRight size={16}/></Link></div>}
